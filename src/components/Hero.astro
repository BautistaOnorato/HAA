<section>
  <div class="background"></div>
  <img src="hero/title.png" alt="" class="title">
  <p>LUNES A VIERNES <span>DE 21 A 23 HS</span></p>
</section>
<style>
  section {
    display: flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    position: relative;
    height: 88vh;
    overflow: hidden;
    flex-direction: column;
  }

  .background {
    width: 100%;
    height: 100%;
    position: absolute;
    z-index: -1;
    background-image: url("/hero/hero_1.jpg");
    background-repeat: no-repeat;
    background-position: center 30%;
    background-size: cover;
    filter: brightness(50%);
  }

  .title {
    width: 60%;
  }

  p {
    font-family: var(--secondary_font);
    font-weight: 400;
    font-size: 2rem;
    color: #eeeeee;
    text-align: center;
    text-transform: uppercase;
    position: absolute;
    z-index: 0;
    bottom: 0;
    padding-bottom: 3rem;
  }

  span {
    font-weight: 700;
    color: var(--primary_text);
  }

  @media screen and (max-width: 900px) {
    section {
      height: 50vw;
    }

    .title {
      width: 80%;
    }

    p {
      font-size: var(--paragraph_size);
      padding-bottom: 1rem;
    }
  }

  @media screen and (max-width: 620px) {
    p {
      font-size: var(--paragraph_mobile_size);
      padding-bottom: 0.5rem;
      
    }
  }
</style>
